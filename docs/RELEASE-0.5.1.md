## 0.5.1

修复“搜索档位”下拉框挤压左侧标签、导致中文逐字换行的问题。标签按文字宽度保留，下拉框随剩余空间伸缩，不设置固定宽度。

已验证中英文卡片在 320、390、768、1280 像素视口下的布局。兼容 DSH 0.1.5-rc.2。

安装：`dsh plugin --profile web add dsh-tavily-provider@0.5.1`，随后重启 DSH 并刷新页面。
